import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
    cardContent: {
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'center'
    },
    buttons: {
        display: 'flex'
        
    }
    
}));